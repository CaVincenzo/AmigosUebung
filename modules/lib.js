const brand = {
    brand: "EnzoCode",
    website: "ichHabeKeinPlan.de"
};

// export const add = (a,b) => a +b;
//
// export const sub = (a,b) => a - b;

 const add = (a,b) => a +b;

 const sub = (a,b) => a - b;


export {
    add, sub
}


export default brand;